[project]
name = "food-order-tracker"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "Flask==3.1.3",
    "openpyxl==3.1.5",
    "psycopg2-binary==2.9.12",
]

[tool.vercel]
entrypoint = "run:app"
